/**
 * Package containing the extraction of each county's name, population, latitude and longitude. To use, just instantiate
 * the CityParser class and call its getCities() method.
 */
package cityparser;